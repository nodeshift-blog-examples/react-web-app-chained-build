{
  "name": "react-web-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1"
  },
  "devDependencies": {
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && npm run copy-nginx",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "copy-nginx":"cp nginx.conf ./build"
  }
}
